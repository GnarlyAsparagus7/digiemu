diff --git a/qemu/target/m68k/helper.c b/qemu/target/m68k/helper.c
index 98463eb..eb32fec 100644
--- a/qemu/target/m68k/helper.c
+++ b/qemu/target/m68k/helper.c
@@ -167,6 +167,12 @@ void HELPER(set_macsr)(CPUM68KState *env, uint32_t val)
     uint64_t regval;
     int i;
     if ((env->macsr ^ val) & (MACSR_FI | MACSR_SU)) {
+        /*
+         * The physical registers are ACCn and ACCextn; only how they form a
+         * 48-bit value changes with the mode (RM 5.3). Unpack by the OLD
+         * mode and repack by the NEW one -- repacking by the old mode made
+         * this a no-op. MACSR[S/U] clear is signed integer (Table 5-3).
+         */
         for (i = 0; i < 4; i++) {
             regval = env->macc[i];
             exthigh = regval >> 40;
@@ -177,10 +183,10 @@ void HELPER(set_macsr)(CPUM68KState *env, uint32_t val)
                 acc = regval;
                 extlow = regval >> 32;
             }
-            if (env->macsr & MACSR_FI) {
+            if (val & MACSR_FI) {
                 regval = (((uint64_t)acc) << 8) | extlow;
                 regval |= ((int64_t)exthigh) << 40;
-            } else if (env->macsr & MACSR_SU) {
+            } else if (!(val & MACSR_SU)) {
                 regval = acc | (((int64_t)extlow) << 32);
                 regval |= ((int64_t)exthigh) << 40;
             } else {
@@ -629,7 +635,8 @@ uint64_t HELPER(macmuls)(CPUM68KState *env, uint32_t op1, uint32_t op2)
     int64_t product;
     int64_t res;
 
-    product = (uint64_t)op1 * op2;
+    /* Signed integers: a two's complement product (RM 5.3.5). */
+    product = (int64_t)(int32_t)op1 * (int32_t)op2;
     res = (product << 24) >> 24;
     if (res != product) {
         env->macsr |= MACSR_V;
@@ -663,21 +670,30 @@ uint64_t HELPER(macmulu)(CPUM68KState *env, uint32_t op1, uint32_t op2)
 
 uint64_t HELPER(macmulf)(CPUM68KState *env, uint32_t op1, uint32_t op2)
 {
+    /*
+     * Signed fractions (MCF5441x RM 5.3.5): product[63:0] = (Y * X) << 1,
+     * convergently rounded (R/T) or truncated to product[63:24], then
+     * sign-extended into the 48-bit accumulator -- except -1 * -1, whose
+     * +1.0 is zero-filled. The operands are two's complement, not unsigned,
+     * and the << 1 aligns the binary point.
+     */
     uint64_t product;
+    uint64_t field;
     uint32_t remainder;
 
-    product = (uint64_t)op1 * op2;
-    if (env->macsr & MACSR_RT) {
-        remainder = product & 0xffffff;
-        product >>= 24;
-        if (remainder > 0x800000)
-            product++;
-        else if (remainder == 0x800000)
-            product += (product & 1);
-    } else {
-        product >>= 24;
+    product = (uint64_t)((int64_t)(int32_t)op1 * (int32_t)op2) << 1;
+    remainder = product & 0xffffff;
+    field = product >> 24;
+    if ((env->macsr & MACSR_RT) &&
+        (remainder > 0x800000 ||
+         (remainder == 0x800000 && (field & 1)))) {
+        field++;
     }
-    return product;
+    field &= (1ull << 40) - 1;
+    if (op1 == 0x80000000 && op2 == 0x80000000) {
+        return field;
+    }
+    return (uint64_t)(((int64_t)(field << 24)) >> 24);
 }
 
 void HELPER(macsats)(CPUM68KState *env, uint32_t acc)
@@ -695,9 +711,10 @@ void HELPER(macsats)(CPUM68KState *env, uint32_t acc)
             /*
              * The result is saturated to 32 bits, despite overflow occurring
              * at 48 bits.  Seems weird, but that's what the hardware docs
-             * say.
+             * say. Saturate in the direction of the true sum: the wrapped
+             * value's sign is the opposite one (RM 5.3.5).
              */
-            result = (result >> 63) ^ 0x7fffffff;
+            result = tmp < 0 ? (int64_t)~0x7fffffffll : 0x7fffffffll;
         }
     }
     env->macc[acc] = result;
@@ -738,7 +755,13 @@ void HELPER(macsatf)(CPUM68KState *env, uint32_t acc)
     if (env->macsr & MACSR_V) {
         env->macsr |= MACSR_PAV0 << acc;
         if (env->macsr & MACSR_OMC) {
-            result = (result >> 63) ^ 0x7fffffffffffll;
+            /*
+             * Saturate in the direction of the true sum, to the 32-bit
+             * fraction limits with the extension bytes clear (MCF5441x RM
+             * 5.3.5): 0x007f_ffff_ff00 or 0xff80_0000_0000.
+             */
+            result = sum < 0 ? (int64_t)0xffffff8000000000ull
+                             : (int64_t)0x007fffffff00ll;
         }
     }
     env->macc[acc] = result;
@@ -756,12 +779,13 @@ void HELPER(mac_set_flags)(CPUM68KState *env, uint32_t acc)
     if (env->macsr & (MACSR_PAV0 << acc)) {
         env->macsr |= MACSR_V;
     }
+    /* EV: the result left ACC[47:39] (fractional) or ACC[47:31] (signed). */
     if (env->macsr & MACSR_FI) {
-        val = ((int64_t)val) >> 40;
+        val = ((int64_t)val) >> 39;
         if (val != 0 && val != -1)
             env->macsr |= MACSR_EV;
-    } else if (env->macsr & MACSR_SU) {
-        val = ((int64_t)val) >> 32;
+    } else if (!(env->macsr & MACSR_SU)) {
+        val = ((int64_t)val) >> 31;
         if (val != 0 && val != -1)
             env->macsr |= MACSR_EV;
     } else {
@@ -869,61 +893,49 @@ void HELPER(flush_flags)(CPUM68KState *env, uint32_t cc_op)
 
 uint32_t HELPER(get_macf)(CPUM68KState *env, uint64_t val)
 {
-    int rem;
-    uint32_t result;
+    /*
+     * The fractional accumulator is a signed 48-bit value
+     * {ext[15:8], ACC[31:0], ext[7:0]} (MCF5441x RM 5.2.1, 5.3.1.1), so it
+     * is shifted arithmetically. A logical shift made every negative
+     * accumulator a huge positive number, which OMC then stored as 0.
+     * S/U set: round to a 16-bit fraction on the low 24 bits, zero-filled
+     * upper word. S/U clear: a 32-bit fraction, rounded on the low 8 bits
+     * only when R/T is set. OMC saturates what does not fit.
+     */
+    int64_t acc = (int64_t)val;
+    int64_t q;
+    uint32_t rem;
 
     if (env->macsr & MACSR_SU) {
-        /* 16-bit rounding.  */
-        rem = val & 0xffffff;
-        val = (val >> 24) & 0xffffu;
-        if (rem > 0x800000)
-            val++;
-        else if (rem == 0x800000)
-            val += (val & 1);
-    } else if (env->macsr & MACSR_RT) {
-        /* 32-bit rounding.  */
-        rem = val & 0xff;
-        val >>= 8;
-        if (rem > 0x80)
-            val++;
-        else if (rem == 0x80)
-            val += (val & 1);
-    } else {
-        /* No rounding.  */
-        val >>= 8;
-    }
-    if (env->macsr & MACSR_OMC) {
-        /* Saturate.  */
-        if (env->macsr & MACSR_SU) {
-            if (val != (uint16_t) val) {
-                result = ((val >> 63) ^ 0x7fff) & 0xffff;
-            } else {
-                result = val & 0xffff;
-            }
-        } else {
-            if (val != (uint32_t)val) {
-                result = ((uint32_t)(val >> 63) & 0x7fffffff);
-            } else {
-                result = (uint32_t)val;
-            }
+        rem = acc & 0xffffff;
+        q = acc >> 24;
+        if (rem > 0x800000 || (rem == 0x800000 && (q & 1))) {
+            q++;
         }
-    } else {
-        /* No saturation.  */
-        if (env->macsr & MACSR_SU) {
-            result = val & 0xffff;
-        } else {
-            result = (uint32_t)val;
+        if ((env->macsr & MACSR_OMC) && q != (int16_t)q) {
+            q = acc < 0 ? -0x8000 : 0x7fff;
         }
+        return (uint32_t)q & 0xffff;
+    }
+    rem = acc & 0xff;
+    q = acc >> 8;
+    if ((env->macsr & MACSR_RT) &&
+        (rem > 0x80 || (rem == 0x80 && (q & 1)))) {
+        q++;
+    }
+    if ((env->macsr & MACSR_OMC) && q != (int32_t)q) {
+        q = acc < 0 ? INT32_MIN : INT32_MAX;
     }
-    return result;
+    return (uint32_t)q;
 }
 
 uint32_t HELPER(get_macs)(uint64_t val)
 {
+    /* Saturate a signed accumulator that does not fit in 32 bits. */
     if (val == (int32_t)val) {
         return (int32_t)val;
     } else {
-        return (val >> 61) ^ ~SIGNBIT;
+        return (int64_t)val < 0 ? SIGNBIT : ~SIGNBIT;
     }
 }
 
diff --git a/qemu/target/m68k/translate.c b/qemu/target/m68k/translate.c
index ec6a39c..3e86aed 100644
--- a/qemu/target/m68k/translate.c
+++ b/qemu/target/m68k/translate.c
@@ -5665,6 +5665,13 @@ DISAS_INSN(fsave)
     }
 }
 
+/*
+ * In integer mode MACSR[S/U] CLEAR means signed and SET means unsigned
+ * (MCF5441x RM Table 5-3). In fractional mode operands are always signed
+ * and S/U only selects 16-bit rounding on stores.
+ */
+#define MACSR_SIGNED_INT(macsr) (!((macsr) & (MACSR_FI | MACSR_SU)))
+
 static inline TCGv gen_mac_extract_word(DisasContext *s, TCGv val, int upper)
 {
     TCGContext *tcg_ctx = s->uc->tcg_ctx;
@@ -5674,7 +5681,7 @@ static inline TCGv gen_mac_extract_word(DisasContext *s, TCGv val, int upper)
             tcg_gen_andi_i32(tcg_ctx, tmp, val, 0xffff0000);
         else
             tcg_gen_shli_i32(tcg_ctx, tmp, val, 16);
-    } else if (s->env->macsr & MACSR_SU) {
+    } else if (MACSR_SIGNED_INT(s->env->macsr)) {
         if (upper)
             tcg_gen_sari_i32(tcg_ctx, tmp, val, 16);
         else
@@ -5767,7 +5774,7 @@ DISAS_INSN(mac)
     if (s->env->macsr & MACSR_FI) {
         gen_helper_macmulf(tcg_ctx, s->mactmp, tcg_ctx->cpu_env, rx, ry);
     } else {
-        if (s->env->macsr & MACSR_SU)
+        if (MACSR_SIGNED_INT(s->env->macsr))
             gen_helper_macmuls(tcg_ctx, s->mactmp, tcg_ctx->cpu_env, rx, ry);
         else
             gen_helper_macmulu(tcg_ctx, s->mactmp, tcg_ctx->cpu_env, rx, ry);
@@ -5776,7 +5783,11 @@ DISAS_INSN(mac)
             tcg_gen_shli_i64(tcg_ctx, s->mactmp, s->mactmp, 1);
             break;
         case 3:
-            tcg_gen_shri_i64(tcg_ctx, s->mactmp, s->mactmp, 1);
+            /* Signed products shift the sign bit in (RM 5.3.5). */
+            if (MACSR_SIGNED_INT(s->env->macsr))
+                tcg_gen_sari_i64(tcg_ctx, s->mactmp, s->mactmp, 1);
+            else
+                tcg_gen_shri_i64(tcg_ctx, s->mactmp, s->mactmp, 1);
             break;
         }
     }
@@ -5808,7 +5819,7 @@ DISAS_INSN(mac)
 
     if (s->env->macsr & MACSR_FI)
         gen_helper_macsatf(tcg_ctx, tcg_ctx->cpu_env, tcg_const_i32(tcg_ctx, acc));
-    else if (s->env->macsr & MACSR_SU)
+    else if (MACSR_SIGNED_INT(s->env->macsr))
         gen_helper_macsats(tcg_ctx, tcg_ctx->cpu_env, tcg_const_i32(tcg_ctx, acc));
     else
         gen_helper_macsatu(tcg_ctx, tcg_ctx->cpu_env, tcg_const_i32(tcg_ctx, acc));
@@ -5840,7 +5851,7 @@ DISAS_INSN(mac)
             tcg_gen_add_i64(tcg_ctx, MACREG(acc), MACREG(acc), s->mactmp);
         if (s->env->macsr & MACSR_FI)
             gen_helper_macsatf(tcg_ctx, tcg_ctx->cpu_env, tcg_const_i32(tcg_ctx, acc));
-        else if (s->env->macsr & MACSR_SU)
+        else if (MACSR_SIGNED_INT(s->env->macsr))
             gen_helper_macsats(tcg_ctx, tcg_ctx->cpu_env, tcg_const_i32(tcg_ctx, acc));
         else
             gen_helper_macsatu(tcg_ctx, tcg_ctx->cpu_env, tcg_const_i32(tcg_ctx, acc));
@@ -5885,7 +5896,7 @@ DISAS_INSN(from_mac)
         gen_helper_get_macf(tcg_ctx, rx, tcg_ctx->cpu_env, acc);
     } else if ((s->env->macsr & MACSR_OMC) == 0) {
         tcg_gen_extrl_i64_i32(tcg_ctx, rx, acc);
-    } else if (s->env->macsr & MACSR_SU) {
+    } else if (MACSR_SIGNED_INT(s->env->macsr)) {
         gen_helper_get_macs(tcg_ctx, rx, acc);
     } else {
         gen_helper_get_macu(tcg_ctx, rx, acc);
@@ -5961,7 +5972,7 @@ DISAS_INSN(to_mac)
     if (s->env->macsr & MACSR_FI) {
         tcg_gen_ext_i32_i64(tcg_ctx, acc, val);
         tcg_gen_shli_i64(tcg_ctx, acc, acc, 8);
-    } else if (s->env->macsr & MACSR_SU) {
+    } else if (MACSR_SIGNED_INT(s->env->macsr)) {
         tcg_gen_ext_i32_i64(tcg_ctx, acc, val);
     } else {
         tcg_gen_extu_i32_i64(tcg_ctx, acc, val);
@@ -5997,7 +6008,7 @@ DISAS_INSN(to_mext)
     acc = tcg_const_i32(tcg_ctx, (insn & 0x400) ? 2 : 0);
     if (s->env->macsr & MACSR_FI)
         gen_helper_set_mac_extf(tcg_ctx, tcg_ctx->cpu_env, val, acc);
-    else if (s->env->macsr & MACSR_SU)
+    else if (MACSR_SIGNED_INT(s->env->macsr))
         gen_helper_set_mac_exts(tcg_ctx, tcg_ctx->cpu_env, val, acc);
     else
         gen_helper_set_mac_extu(tcg_ctx, tcg_ctx->cpu_env, val, acc);
